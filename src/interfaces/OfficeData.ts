export interface OfficeData {
  id: string;
  officeName: string;
  physicalAddress: string;
  emailAddress: string;
  phoneNumber: string;
  maximumCapacity: number;
  officeColor: string;
  staffCount: number;
}